Q- Given an integer, return true if the integer ends with 0, otherwise return false.
   isEndWithZero(12) == false
   isEndWithZero(1230) == true

(Using Math.mod function)
public Boolean isEndWithZero(Integer num){
     //code here
     //Using Math.mod to check if the number ends with 0
     return Math.mod(num, 10) == 0;
}
